---

applications:
- name: cf-storagegrid-broker   
  stack: cflinuxfs3
  routes:
  - route: ((route))
  instances: ((instances))
  memory: 32M
  disk_quota: 32M
  health-check-type: port
  buildpacks:
  - go_buildpack